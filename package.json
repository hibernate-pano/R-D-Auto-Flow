{
  "name": "rnd-auto-flow",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.0.9",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm typecheck",
    "test": "vitest run",
    "dev:api": "pnpm --filter @rdaf/api dev",
    "dev:web": "pnpm --filter @rdaf/web dev"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3",
    "vitest": "^4.0.8"
  }
}
